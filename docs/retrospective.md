# 项目复盘 — 跨境供应链延误风险控制塔 v0.2

周期：2026-07-06 密集完成（原计划 6 周，10h+/周）｜路线：B（自研原型学原理）

## 1. 目标达成核对（对 plan §10 KPI）

| KPI | 目标 | 实际 |
| --- | --- | --- |
| 风险查全率（高危漏判=0） | ≥95% | 1.000，漏判 0 |
| 风险查准率 | ≥85% | 1.000 |
| 影响定位准确率（设计案例） | 100% | 51/51 全对 |
| 闭环完整性（可追溯链） | 100% | C1/C2 通过，含被拒调用留痕 |
| 演示脚本 | ≤5 分钟 | 人工走查通过（C3） |
| AI 评估 | 10 题溯源+0 编造+越权拒绝 | 11 题全过 |

24/24 演示断言关闭。满分的诚实注脚见 weekly-notes/W4：评估的价值在防回归。

## 2. 学习目标自查（plan §12 学习指标 → 证据）

1. **从场景反推对象模型**：从"延误击穿承诺"反推出行级粒度与 ShipmentAllocation 枢纽（D1/D3）
2. **表字段 vs 对象属性**：D5 的判据——只有需要独立生命周期/动作/权限的概念才配当对象
3. **设计 Link**：影响传播链一条 SQL 走通就是 link 设计的验收
4. **业务决策封装成 Action**：A3-A6 五要素（权限/前置/成功/失败/审计），动作在动作层不在按钮里
5. **识别落地的坑**：见 §3——每个坑都有编号有出处
6. **AI 基于对象和动作工作**：工具白名单+确定性简报保证溯源，靠架构不靠模型自觉

## 3. 踩过的坑（真实教训，按周）

- **W2 可复现性破坏**：模块常量被原地污染——进世界的数据必须复制。"同种子逐字节一致"
  这条硬验收当场抓住它，验收纪律的第一次回报
- **W2 随机涌现不可靠**："多客户击穿 ≥15"靠调参是掷骰子，最终三级确定性补齐——
  计划里写"人为设计"的东西就该确定性保证
- **W4 D8 时间泄漏**：引擎偷用快照日状态，时钟拨回后"未来事件"仍可见——
  凡带时间语义的读取必须显式声明以何时的世界为准
- **W4 断言与规则相撞**：R3 正确报警但场景设计没料到——反例断言要对全部规则交叉检查；
  规则没错时不要动规则
- **W5 UI 回执被冲掉**：用户看不到成功就会重复点击——动作有回执才算完成，对人对 AI 一样
- **全程**：ChatGPT 初始方案的三大缺陷（无行级粒度、milestone 未对象化、KPI 不可测）
  印证了"先最小闭环"与 ground truth 驱动评估的价值

## 4. 治理机制的实战记录

决策日志 D1-D11 全程 append-only；人裁决 C1-C6、D10、D11 三次；AI 越权尝试
（人误点 + 评估诱导）全部被拒且留审计；每周验收对断言打勾未对感觉打勾。
AGENTS.md 的规则没有一条是摆设——每条都至少被触发过一次。

## 5. v0.3 方向（不承诺）

1. 实现 v0.1 SKU 准入闭环，与控制塔共享 Supplier/Sku/Customer——验证 ontology 复用性
   （Palantir 模式的核心卖点）
2. Container 对象化（一票多柜）+ Invoice/费用异常闭环 + incoterm 驱动的成本责任划分
3. 单证号级 entity resolution（G1 完全体：删掉源表 shipment_id）
4. 规则引擎 → 有监督风险评分（ground truth 已备）
5. UI 回放滑条（as_of 滑动看世界演化）；任务超期升级
