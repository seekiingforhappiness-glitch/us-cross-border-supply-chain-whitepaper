# W3 复盘 — 管道与对象层（2026-07-06）

## 交付物

- `pipeline/`：er.py（供应商名称归一）+ build_ontology.py（对象层重建）+ evaluate.py（精度评估）
- `data/ontology.sqlite`：10 张对象表 + supplier_name_map + 3 张 W4/W5 空表（risk_events/tasks/action_log）
- `data/dq_report.json`：数据质量报告
- `docs/data-guide.md`：小白数据导读（按 DEMO-01 故事线讲九张源表）

## 评估结果（python3 -m pipeline.evaluate，全绿）

- 重建精度：shipment status/eta_current/customs_status 120/120，line_status 705/705（对照 truth 快照）
- 影响传播链：milestone→shipment→allocation→line→SO→customer 一条 SQL 走通（DEMO-01 到 CUS-0007）
- 噪声处理：30 条重复事件标记不删（事件不可变）、33 对乱序按 event_time 消解、
  9 处状态冲突以事件流为准纠正、ER 8/8 命中

## 数据质量报告要点

输入 653 条 milestone（含 30 重复）；空值率 vessel/carrier ≈9%；孤儿分配 0；
ER 发现 1 个规范名歧义（"dongguan botai" 两家归一后同名——真实世界同名坑，记录在案）。

## 架构要点

1. **状态一律从事件流推导，不信 tms 状态字段**（status_source 保留原值供审计）——DEMO-11 陷阱由此通过
2. 乱序安全：推导前按 event_time 全排序，ingested_at 只用于判重保序
3. 重复事件标记 is_duplicate 保留不删——对象层事件不可变原则
4. pipeline 全程未读 data/truth/（评估脚本除外，其属评估层）

## 同日完成的数据真实感升级（应人的要求）

ISO 6346 柜号（箱主代码按船司 + 校验位，可在 bic-code.org 验证）、船名池按船司、
真实客户/SKU 商号、PO 状态与 shipment 联动、多客户击穿三级确定性保证（终极后备：延误注入）。

## 已知限制

- ER 用规范化+令牌匹配（限时实现），歧义名靠人工裁决，未上模糊算法——符合 plan §13 限时约定
- so_lines 705 与 plan 估算 800 有偏差（随机权重所致，在 ±20% 容差内）
