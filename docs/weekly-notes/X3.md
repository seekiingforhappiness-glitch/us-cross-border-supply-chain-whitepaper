# X3 复盘 — 费用引擎与闭环（2026-07-07，Opus 执行/Fable 评审）

## 交付（经独立复核）

- engine/cost_rules.py：R4 超收/R5 重复/R6 计划外，as_of 纪律（9 张未来发票不参与）；
  MatchInvoice 副作用+审计置于 CreateRiskEvent 之前（子代理按状态机因果自行纠正时序，合理）
- app/actions.py：三种费用提案 + G4 转嫁门禁（REBILL_MATRIX 与 ontology JSON 同步）+
  发票回写；既有三类提案行为零变化
- 费用工作台 UI + 风险详情账单行表；六角色 AppTest 无异常
- engine/evaluate_cost：R4-R6 Recall/Precision 1.000（25/25，value/severity/行集合全对账）
- app/test_cost_loop：XC1-XC6 全绿（含 CD-D 的 DDP rebill 被 G4 拒且审计）

## F1 复用命题的兑现时刻

同一张风险队列流动 6 种规则事件；费用异常的派单/提案/审批/关闭/审计与延误风险
走同一套机制——X3 新增的只有：1 个检测模块、3 个提案类型、1 个门禁、1 个 UI 标签页。

## 评审记录

- 子代理 7 项歧义裁决全部认可，其中"MatchInvoice 时序前置"属主动语义纠正
- 唯一授权的评估器修改（evaluate.py 加 R1-R3 过滤）核实为最小改动且带授权注释
- engine/evaluate.py 的范围过滤属职责划分澄清，非指标操纵——R4-R6 有专属评估器
