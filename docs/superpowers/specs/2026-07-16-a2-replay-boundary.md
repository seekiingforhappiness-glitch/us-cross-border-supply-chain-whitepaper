# A-2 时间轴回放·诚实边界清单（执行记录，V13② 落地）

日期：2026-07-16 ｜ 原则：**多数对象表只存当前态，假装能完整回放=造假**。能按时点真算的才回放，
不能的显示当前值并挂灰标「显示当前值」，绝不给假历史数字。API 信封 `current_state_only`
字段完整列出当次不可回放项。

## 可回放（按时间戳真重算）

| 指标 | 世界 | 依据 |
|---|---|---|
| 在途票数 | 两世界 | 里程碑不可变事件流（离港≤时点 且 无到港/交付≤时点） |
| AI 今日检测/提案 | 两世界 | detected_at / action_log 逐日复算（验证世界批量单日→过去日=0，是测量非造假） |
| ai-flow 事件流 | 两世界 | 各来源按自身时间戳过滤 |
| 逾期应收/应付、14天净流出、超期任务 | 两世界 | due_date/due_at 真排期回算 |
| 费用敞口/单一依赖/对账/客户敞口集/库存可救集/panorama 锚定/趋势 | 仅模拟世界 | 按 detected_at/resolved_at 重建"截至当日未闭环"（模拟世界有 14 个月真历史；验证世界为单日快照不重建） |

## 不可回放（显示当前值+灰标）

OTD 累计率、清关卡点、延误直方、毛利率分布、在途货值、应收应付存量水位、供应商交期/缺陷率、
库存击穿/盘点差异、准入漏斗/健康度/敞口金额、待拍板存量、AI 累计、panorama 节点/边
——对象表无历史版本。补齐需事件溯源/版本化存储（波2 Command 事件流的自然延伸，届时重评）。

## 已知限制

- 下钻详情页未逐子指标加灰标（顶栏回放态+卡级标注+API 信封已传达；逐指标标注候需要再扩）。
- 浏览器合成拖拽不触发"松手提交"（真人鼠标/触控正常；播放键与点击路径已亲验全通）。
